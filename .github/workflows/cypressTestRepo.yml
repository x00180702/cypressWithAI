name: ci
on: [push, pull_request]
jobs:
  install:
    runs-on: ubuntu-20.04
    steps:
      - name: Check out code 🛎
        uses: actions/checkout@v2
      - name: Install dependencies 📦
        uses: cypress-io/github-action@v2
        with:
          # just perform install
          runTests: false
  
  tests2:        
    runs-on: ubuntu-20.04
    needs: install
    steps:
      - name: Run tests with "@tag1" 🧪
        run: |
         npx cypress run --env tags="@bpTest"
